# SignalScout

Turns a plain-language request into an automated webpage watch that alerts you when something changes. Still early — being built incrementally, more detail coming once the core system is done.